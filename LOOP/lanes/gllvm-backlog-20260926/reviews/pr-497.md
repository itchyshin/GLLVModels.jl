# Review: PR #497 — true-parity decision packet (2026-09-26 update)

**Head:** 06de98f4c (branch `claude/decision-packet-20260926`)
**Diff (three-dot, against `origin/main`):** 1 file, 106 insertions, 0 deletions —
`docs/dev-log/owed/2026-09-25-true-parity-decision-packet.md` (new on main; brought over
from the paused `claude/lane-true-parity-finish-20260925` lane, which never merged).
**Verdict: MERGE**

## What it claims
Brings the 75-line decision packet from the paused `true-parity-finish` lane onto `main`
verbatim (items 1–23: four unlocking answers, the #485 decision, 11 scope decisions, R writes,
compute campaigns, final sign-off), then adds 13 new items (24–36): the 11 NEEDS-DECISION
issues from PR #495's cross-audit triage, plus the two live divergences #129 and #131 that
gate-tier extractors depend on. Each new item states a QUESTION, a RECOMMENDATION and a
drafted one-line reply. States no code was touched, no frozen-contract/required-cell/
ledger-signature file was touched, no Project.toml/tag/release change, no claim of true
parity. Opened as a draft.

## Checks performed
- **Verbatim retention of items 1–23, byte-for-byte.** `diff` between this file and
  `docs/dev-log/owed/2026-09-25-true-parity-decision-packet.md` as it exists on
  `origin/claude/lane-true-parity-finish-20260925` (the source branch cited in the PR body,
  fetched and read read-only from a separate worktree): **zero differences in the first 70
  lines.** The new material (13 items) is inserted as its own section before the existing
  "Final sign-off (B-14)" section rather than appended after it — a sensible ordering choice
  (sign-off logically belongs last) that does not touch a single character of the original 75
  lines. The PR body's "unchanged... appended/kept exactly as written" claim holds.
- **All 13 new items carry all three required fields.** Both new tables (11
  NEEDS-DECISION rows as items 24–34, 2 live-divergence rows as items 35–36) have `# | Question
  | Recommendation | Reply to paste` columns populated for every row — no gaps.
- **All 14 cited issue numbers resolve, and resolve to what the item says.** `gh issue view`
  on `itchyshin/GLLVModels.jl` for #133, #136, #139, #143, #146, #150, #152, #153, #156, #158,
  #161, #129, #131, #142 — every title matches the item's description (e.g. #133 "Ordinal
  location parameterization differs..." matches item 24's ordinal-cutpoints question; #129
  "Julia Wald and profile CIs disagree on sigma_phy scale" matches item 35 exactly). This is
  in clear contrast to PR #496, where one issue citation (#1192) does not match its claim —
  none of that failure mode appears here.
- **Spot-checked the code-line citations behind the two "live divergence" items against
  `origin/main`, since these carry the strongest claims (numeric reproductions, a
  recommended fix).** `confint.jl:103-105` reads `sigma_phy` as `:linear` (Wald); `
  confint_profile.jl:119-120` reads it as `:log_sd` (profile) — confirmed, matches item 35's
  claimed inconsistency exactly. `confint_derived.jl:208` (`communality`) and `:280`
  (`correlation`) both divide by `sigma_y_site(fit)`, the full per-site covariance — confirmed,
  matches item 36. Also checked `src/families/ordinal.jl:1-9` (shared cutpoints, no per-trait
  intercept — matches item 24), `src/bridge.jl:485-490` (`d::Integer = 1` — matches item 26),
  `src/likelihood.jl:141-146` (`Λ_phy` size guard exists; item 29 correctly says it's `Λ_phy`,
  not `Λ_B`) and `src/likelihood.jl:377-390` (identity-link `σ_phy`, sign-anchor comment —
  matches item 25). All match.
- **`slop_check.py` reproduces exactly.** `python3 ~/shinichi-brain/tools/slop_check.py` on
  the packet file at the PR head: `7 hits / 3003 words = 2.3 per 1000 (em dashes 0/1000)`,
  `FINDINGS: 0` — matches the PR body's claimed numbers exactly, verdict green.
- **No em dashes** (`grep -c "—"` → 0). **Agent-mention check clean**
  (`python3 ~/shinichi-brain/tools/agent_mention_check.py --text <body>`).
- **Hard rules respected.** Diff touches only the one new doc file — no code,
  `Project.toml`, tag, frozen-contract, required-cell, or ledger-signature file. No claim of
  achieved true parity ("True parity is defined by seven clauses..." names the definition,
  it doesn't claim satisfaction). No sign-off is asserted on the maintainer's behalf — the
  packet asks questions and drafts replies, it doesn't answer them.

## Findings (should-fix, non-blocking)
1. **The "confirmed still live" claim for #129/#131 is pinned to a stale ref.** Both items
   say "confirmed still live on `origin/main` @ `d9bc77412`" — that commit is a real point on
   `main`'s history (`git merge-base --is-ancestor` confirms it), but `main` has since gained
   24 more commits (through #487/#488/#492/#494/#495 and others). The claim is accurately
   scoped to the ref it names — it is not overclaiming — but neither #129 nor #131 has been
   re-verified against the current tip. Given these directly gate a recommended code fix
   (item 35 tells the reader to edit `confint_profile.jl`), the maintainer or the next lane
   should re-run the reproduction against current `main` before acting on the recommendation,
   not just before signing the packet.
2. Same staleness applies one level up: PR #495 itself (the source of all 13 new items) was
   triaged against `origin/main @ d9bc77412`, a ref 24 commits behind current `main`. None of
   the intervening commits (per their titles: Gamma/Beta/cov mode-search fixes, a docstring
   reword, #488/#495 merges themselves) look related to any of the 13 items, but this wasn't
   independently re-verified item-by-item against current `main` — reasonable given the time
   budget, but worth naming as a residual gap before final sign-off.

## Bottom line
Clean. Verbatim retention of the untouched packet confirmed byte-for-byte; every new item is
structurally complete (question/recommendation/reply); all 14 issue citations and the code-line
citations behind them resolve to exactly what's claimed; `slop_check` and the agent-mention
check both reproduce as stated; no hard rule is touched. The only gap is staleness (should-fix,
not blocking): the two live-divergence items were last confirmed against a ref now 24 commits
behind `main`'s tip and should be re-run before anyone acts on the recommended fix.

## Scoreboard audit (docs/dev-log/core070/true-parity-gate-tier-scoreboard-2026-09-25.md)
See the companion report `rose-audit-scoreboard-packet.md` for the full write-up. Summary:
32/32 rows present with the claimed category breakdown (A:15, B:4, C:5, D:8); count-summary
table (19 PARTIAL, 13 OPEN, 0 EVIDENCED, 0 DISPOSITION-SIGNED) matches an independent row-by-row
tally exactly. Spot-checked 11 rows (all 5 PARTIAL rows that cite a PASS receipt — A2, A5, A7,
A11, C1 — plus A9, A12, A14, B1, D6, D8): every cited receipt file, JSON field, quoted string,
and `gh issue`/`gh pr` state resolves to what the row claims, with one exception — A9 cites
"PR #483 (open, needs maintainer review)", which was true at the scoreboard's own measurement
ref (`d9bc77412`, 2026-09-25) but PR #483 has since **merged** (confirmed via `gh pr view`).
The scoreboard is honestly ref-pinned, so this isn't a false claim, but the document is now
24 commits stale on `main` and should be refreshed (or at minimum have its staleness flagged)
before anyone treats A9/A11's live-PR-status prose as current.
