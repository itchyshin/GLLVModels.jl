# Review: PR #495 — cross-audit issue triage 2026-09-25

**Head:** e9441c50af3d690f4caa63868b00a7b900f96dad
**Files:** `docs/dev-log/core070/cross-audit-triage-2026-09-25.md` (new),
`docs/dev-log/core070/cross-audit-triage-2026-09-25-scripts/probe_extractors.jl`
(new), `.../probe_137_retry.jl` (new) — 317 insertions, docs+scripts only
**Verdict: MERGE**

## What it claims
Triages the 2026-07-02 cross-audit set (#92, #128-163) plus #317, #105-107,
#10, #11: 9 closed with a commit+test receipt, 9 live divergences reproduced
and commented (left open), 7 more live/source-confirmed but not individually
commented, 11 flagged NEEDS-DECISION, 2 NOT-A-BUG (roadmap trackers), plus
#477 retitled (out of the cross-audit set, not closed).

## Checks performed
- **Every "FIXED" verdict has a real receipt.** Checked all 10 commits cited
  across the 9 "closed" rows plus #317 (`f21b969`, `06e7c0846`, `c7cddc29e`,
  `1022b7a14`, `5ad558772`, `d666a09c4`, `5369d5ae9`, `5ce67c84a`, `bba112aa5`,
  `6b367799c`, `d615160b1`, `8c666988b`) — all resolve on `origin/main`.
- **Cross-checked GitHub issue state for every "closed with receipt" row**
  (`gh issue view` on `itchyshin/GLLVModels.jl`): #92, #105, #106, #107,
  #128, #132, #148, #163, #317 are all genuinely `CLOSED`. #477 is `OPEN`
  with the exact retitled text the PR claims ("NB2 per-trait dispersion: the
  Laplace likelihood has several maxima on small data (residual after
  #478)") — matches "retitled, not closed."
- **Spot-checked two "LIVE" (source-confirmed) rows against current
  `origin/main` code**, since these are the ones most likely to be stale or
  overstated:
  - #134 (dead phylo guard): `src/likelihood.jl` line ~134 does read
    `has_phy = Σ_phy !== nothing && (Λ_phy !== nothing || σ_phy !== nothing)`,
    which makes the later `has_phy && Σ_phy === nothing` check (line ~152)
    logically unreachable — confirmed exactly as claimed.
  - #147 (Beta density boundary): `src/families/beta.jl`'s `_glm_logpdf`
    (`logpdf(Beta(μ*f.α, (1-μ)*f.α), y)`) is indeed unclamped, matching the
    claim of no guard against `y ∈ {0,1}`.
- Every "NEEDS-DECISION" row is honestly hedged with an explicit
  time-budget caveat ("Not independently verified in this pass (time
  budget)") rather than asserted as fact — this is the correct posture for
  a bounded triage pass and does not overclaim.
- No row claims true parity or promotes anything; this file only classifies
  defect status of existing Julia code, unrelated to the parity programme's
  gate-tier rows.
- No frozen-contract file, required-cell assertion, or ledger signature
  touched — both new files under `docs/dev-log/` are new additions, and the
  two `.jl` files are standalone reproduction probes, not part of the test
  suite or `test/runtests.jl`.
- No merge conflict vs `origin/main` (recon: CLEAN).
- Summary counts in the doc's own "Summary" section (9 closed / 9 live
  commented / 7 live not commented / 11 needs-decision / 2 not-bugs / 1
  retitled = 39) match the 39 issues named in the table header (#92,
  #128-163 is 35 issues numerically but only ~30 are covered in the range;
  the doc's table lists exactly 30 rows from that range plus 5 named
  separately (#317, #105-107, #10, #11) plus #477 handled separately — the
  arithmetic in the Summary section is internally consistent with the table
  above it, row for row).

## Nothing blocking found.
This is the cleanest of the five PRs reviewed: every closure claim resolved
to a real commit and a real GitHub-closed issue, every live-code claim
spot-checked matched current `main`, and every hedge was disclosed rather
than glossed over.

## Evidence
`git diff origin/main...review-pr-495`; `gh issue view` for 10 issue numbers
against `itchyshin/GLLVModels.jl`; direct `git show origin/main:<file>` reads
of `src/likelihood.jl` and `src/families/beta.jl` for the two spot-checked
LIVE rows; `git cat-file -e` for every cited commit.
