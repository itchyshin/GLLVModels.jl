# Review: PR #488 — capability-status join fixes

**Head:** 23224af3a15638a945d3efa9421c0aa8c843aff6
**Files:** `docs/design/capability-status.md` (135 insertions, 26 deletions, docs-only)
**Verdict: MERGE**

## What it claims
Fixes a parser-breaking table header (`| Capability (twin 0.7.1 vocabulary) |
Status |` — not recognised by `tools/parity_ledger.R`'s parser, silently
dropping 4 rows from every join), splits a false-joining ordinal row
(`ordinal_probit / cumulative_logit` merged into one row when Julia actually
implements `ordinal_probit` and `ordinal_logit` separately), corrects three
stale "twin cut" claims (ZIP/ZINB/ZIB, censored_poisson), and adds two new
sections (Model selection; Grouping levels) with `partial - R pairing not
established` flags. New DIFFER-join summary: 57 matched (22 AGREE/23
R-NARROWER/3 J-NARROWER/9 DIFFER), 23 R-only, 34 Julia-only, CLOSURE FAIL (2
undispositioned rows).

## Checks performed
- Confirmed on `origin/main` the stale header
  `| Capability (twin 0.7.1 vocabulary) | Status |` exists verbatim at line
  561 before this PR, and the PR's new headers are the parser-recognised
  `| Capability | Status |` form.
- Confirmed `tools/parity_ledger.R`'s header comment (in the local read-only
  `gllvmTMB` clone) says `docs/design/capability-status.md` "did not exist at
  the frozen 0.7.0 oracle," i.e. this join is against R's moving `main`, not
  a frozen pin — matches the PR's "Corrected 2026-09-25" note.
- Confirmed `tools/parity_ledger.R`'s own line 8 documents the
  `CLOSURE: PASS / FAIL` mechanism the PR cites, and its capability-status.md
  row `zi_poisson / zi_nbinom2 / zi_binomial ... FAM-21, FAM-22, FAM-23` cell
  matches the PR's claimed R-side row text (including the same
  per-trait-vs-shared-scalar NB2 dispersion divergence language) word for
  word.
- Confirmed `gllvmTMB` PR #1240 (zero-inflated families) and #1254
  (censored_poisson engine) are both MERGED on GitHub, matching the PR's
  claims that these "twin cut" statements are now stale.
- Confirmed `select_lv` at `src/model_selection.jl` and `chibar2_pvalue` /
  `variance_lrt` exported at `src/GLLVModels.jl:249` — the new "Model
  selection" section's code citations are accurate. `test/test_model_selection.jl`
  and `test/test_known_sentinel_defects.jl` both exist.
- No row claims cross-package parity beyond what the note states; every new
  row is explicitly flagged `partial - R pairing not established` or
  `PROPOSED, NOT SIGNED` (the two undispositioned rows are labelled as
  proposals for the maintainer, not self-signed).
- Docs-only change to a single file; no frozen-contract file, ledger
  signature, or required-cell assertion touched.
- No merge conflict vs `origin/main` (recon: CLEAN).

## Nothing blocking found.
The two "PROPOSED, NOT SIGNED" dispositions are honestly labelled as such,
and the PR correctly declines to edit `tools/parity_ledger.R` itself (which
lives in `gllvmTMB`, out of scope for a GLLVModels.jl PR).

## Evidence
Diffed `origin/main...review-pr-488`, cross-checked cited lines/rows against
both the fetched PR head and a read-only look at the local `gllvmTMB` clone's
`tools/parity_ledger.R` (not edited), plus `gh pr view` for #1240/#1254.
