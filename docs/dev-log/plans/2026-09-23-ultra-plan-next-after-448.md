🎯 GOAL
Solo platform: Cursor (this session; lane `claude/lane-s9cov-20260921` → then fresh `cursor/latte-kernel-20260923`)
Deliverable: #448 merged; then a measured kernel-first Latte-gap arc (diagonal precision + identical log-link matrices) with identity gates, no public speed claim until NB2/phylo identity holds
HEADLINE: Close the honest ~4.6× Latte GLMM gap by the KERNEL (diag precision + identical matrices under log link), not by demoting Nelder–Mead alone
IN PARALLEL: owed `grouped-analytic-20260920` gate-check reverify (Totoro; correct BRANCH, not dir name) after #448 lands
DEFER: simplex-only / NM-demotion-alone · Latte/GMRF adoption · DRM speed6 #781 · HSquared speed12 · moment_start until NB2 fixed · public speed claim · edits to coordinated_speed_kickoff
DISCIPLINE: verify=rtol 1e-8 vs origin/main + gllvmTMB on NB2 joint + phylo before any timing quote · compute=Totoro CPU certify (≤100 cores); DRAC only if array · closure=G0 paste + /goal; STOP here (Phases 0–2)

---

# Ultra-plan: NEXT after S9c PR #448 (Phases 0–2 · STOP at G0)

Active lenses: Shannon, Ada (perspectives). Spawned subagents: none.
Plan mode: read-only through G0. Do **not** implement until Shinichi pastes G0.

Unlazy scope: `.unlazy/latte-kernel-after-448-20260923/`
Do **not** edit `docs/dev-log/plans/*coordinated_speed_kickoff*` or resurrect the pre-correction latte GOAL as if true.

---

## Phase 0 — Receipts

### 0.1 PR #448 state (2026-09-23, measured)

| Field | Value |
|---|---|
| Repo | `itchyshin/GLLVModels.jl` |
| URL | https://github.com/itchyshin/GLLVModels.jl/pull/448 |
| State | **OPEN** (not merged) |
| Head SHA | `9788fca770a2a2c38b4fd0cd385d0b43dc191434` (`9788fca77`) |
| Branch | `claude/lane-s9cov-20260921` |
| mergeable | MERGEABLE |
| mergeStateStatus | UNSTABLE (advisory check red) |
| Julia 1.10 shards 1–4 | SUCCESS |
| Julia 1 shards 1–4 | SUCCESS |
| Documenter | SUCCESS |
| Frozen R 0.7.0 family smoke | FAILURE (**advisory**; rebuilt oracle; not a merge blocker by repo policy) |

After-task on lane: `docs/dev-log/after-task/2026-09-22-s9c-coverage-448.md` — two-implementation merge done; focused `gate_coverage` PASS; in-suite 21/21; `RTOL_FD` still `1e-6`; **PR left OPEN for human merge**.

S9c unlazy `.unlazy/s9c-coverage-448-20260922/`: leaves 1–3 PASS (main-tip merge, focused verify, PR hygiene); leaf 4 owed reverify **DEFERRED**.

**Consequence:** Slice 1 of this plan is **merge-when-green #448** before any new arc code.

### 0.2 Lane pre-flight (Shannon)

```
PLATFORM: cursor | ON BRANCH: claude/lane-s9cov-20260921
VERDICT: FOREIGN LANE ACTIVE (claude + codex) + other Cursor PRs live
LANE TAKEN: s9cov for #448 merge-when-green only; latte/kernel work opens a NEW branch after merge
```

Foreign (do not bleed into):
- Claude: `claude/exp-nm-budget-20260922`, reader-arc handover #444, …
- Codex: derived-CI reader #437, public docstrings #433, …
- Cursor paste-gated DRAFTs: #399/#409/#410/#411 (untouched)
- Sister speed: DRM `DRModels.jl` #781 speed6 OPEN (green CI; scaling-sweep skipped/failed — **not THIS lane’s NEXT**)
- HSquared speed12 lane exists locally; open H² PRs are honesty/docs/0.9 — **not NEXT ahead of latte/kernel**

**s9a owed reverify trap:** dir `~/local-scratch/lanes/GLLVM.jl-s9a-hessian-20260921` is currently on branch `claude/exp-nm-budget-20260922` (wrong for the ledger). Ledger also present on `GLLVM.jl-s9cov-20260921` and older speed lanes. **Always `git rev-parse --abbrev-ref HEAD` before `gate-check --scope grouped-analytic-20260920`.** Prefer a clean checkout of `origin/main` (post-#448) or the branch that owns the leaf EXPECT repairs.

### 0.25 Prior-work sweep receipt

| Ask | Finding |
|---|---|
| Is #448 already merged? | **No** — OPEN @ `9788fca77`; first slice = merge |
| Is S9c coverage work done? | **Yes** on branch (after-task + leaf 1–3 PASS); waiting human merge |
| Latte next-arc already planned? | Yes: `~/shinichi-brain/docs/dev-log/plan-actual/2026-09-22-next-arc-close-the-latte-gap.md` — **premise REFUTED**; use **CORRECTION section only** |
| Oracle on disk? | Yes: `~/local-scratch/latte-prerun-20260918/runs/f3_ours_glmm.jl` (not empty) |
| Empty plan stubs on lane? | `2026-09-22-ultra-plan-s9c-coverage-merge.md`, `…-next-slice.md`, `…-compute-routing.md` are empty files — ignore; after-task + unlazy leaves are authoritative |
| DRM / H² speed next instead? | No — those are other engines; GLLVModels’ highest-value measured gap is still the GLMM Latte kernel floor |

### 0.3 Two-bar / roster

Snapshot 2026-09-20: Cursor Models **73%** / Other Models **98%** (gap ≥10pp → prefer **Cursor Models / explicit Grok** for scout + bounded agent slices; judgment to lagging bar). On-demand disabled.

### 0.4 Questions only Shinichi can answer (G0)

1. Merge #448 now despite advisory Frozen-R red? (**Plan assumes YES** — advisory.)
2. Approve kernel-first Latte arc as HEADLINE (not NM demotion / not DRM speed6)?
3. Totoro for retime + identity certify; no DRAC unless an array appears?

Pre-authorisation envelope after G0: scoped local edits, focused Julia tests, Totoro certify, local commits, draft PR on the new branch. Stops: push without naming, merge other PRs, release, public speed claim, credentials, `rm -rf`.

---

## Phase 0.6 — Route check (answered)

1. **Destination (one sentence, end state):** After #448 is on `main`, the grouped non-Gaussian Poisson GLMM path on the Latte 200×5 fixture uses a keyword-off diagonal / identical-matrix kernel that closes the addressable share of the honest ~4.6× gap, with rtol 1e-8 identity on NB2 joint + phylo before any timing is quoted.
2. **Do ≥2 slices depend on undecided either/ors?** No — correction already killed “remove simplex alone”; KERNEL is the headline.
3. **Every slice names a real output path?** Yes — see slice table.

→ **Route clear. Slice list (not a decision map).**

---

## Ranked NEXT (pick ONE primary)

| Rank | Candidate | Verdict |
|---|---|---|
| **1** | **Kernel-first Latte gap** (diag precision + identical log-link matrices; fuse value/grad into analytic inner solve) | **PRIMARY HEADLINE** — highest honest value after correction |
| 2 | Owed `grouped-analytic-20260920` `--reverify --approve` | Parallel hygiene after #448; does not replace (1) |
| 3 | DRM speed6 #781 | Sister engine; leave to DRM lane |
| 4 | HSquared speed12 | Sister engine; not blocking GLLVModels |
| — | Nelder–Mead demotion alone / remove simplex alone | **FORBIDDEN** (measured slower / conflated oracle) |

---

## Slice table (5 max)

| ID | Slice | OWNS (paths) | Compute | Depends | Bar / model |
|---|---|---|---|---|---|
| **S0** | **merge-when-green #448** — confirm advisory Frozen-R only; `gh pr merge` (or Shinichi); record merge SHA on board/check-log | `docs/dev-log/check-log.d/**`, `docs/dev-log/after-task/**` (merge receipt only); **no** `src/` | n/a (GitHub) | — | Cursor Grok / Shannon |
| **S1** | **Paired retime** — re-run `f3_ours_glmm.jl` (≥5 warm reps) + add shipped-side harness with **identical timing scope** (include final grad / Hessian / acceptance if shipped pays them). Fixture md5 `60a293d6…`. Output TSV | `bench/latte_gap_retime/**` or `docs/dev-log/simulation-artifacts/2026-09-23-latte-kernel-retime/**`; harness script under `tools/` or `bench/` | Laptop smoke → **Totoro** if laptop noisy | S0 | Cursor Grok |
| **S2** | **Ablation map** — four mechanisms + **diagonal-kernel** (plan omitted it): optimiser path · scalar kernel · 2-param specialisation · timing scope · **diag / identical Hf≡Ho under log link**. Separate section TSV; no code change that ships | same artifacts dir; read-only `src/grouped_laplace.jl`, `src/grouped_nongaussian_fit.jl` | Laptop | S1 | Cursor Grok |
| **S3** | **Kernel prototype, default OFF** — diag-precision path + skip duplicate factorisation when log-link identical matrices; optionally fuse value+grad in the analytic inner solve. Keyword gated. **No** NM default flip | `src/grouped_laplace.jl`, `src/grouped_nongaussian_fit.jl` (narrow); focused tests under `test/test_grouped_*.jl` | Laptop unit → **Totoro** certify | S2 | Composer/Grok for code; Codex only if HPC stuck |
| **S4** | **Identity before timing** — rtol 1e-8 vs `origin/main` on Poisson + **NB2 joint** + **phylo**; falsify by injection. Only then quote wall times (2 thread regimes). Shinichi decides default ON | tests + TSV + after-task; **no** README speed claim | **Totoro** (D-50; not Actions) | S3 | Codex/Claude for long certify OK |

**IN PARALLEL (after S0, disjoint OWNS):** **P1** — `node …/gate-check.mjs --reverify --approve --scope grouped-analytic-20260920 --jobs 1 --timeout 7200` on a checkout whose **branch** owns the ledger (not the misnamed s9a dir). Host: **Totoro**. Wall ~2–4 h. Does not block S1–S3 design writing; blocks any “S8/S9 ledger closed” claim.

Graft hubs for S2–S3: `joint_grouped_laplace_loglik` (`src/grouped_laplace.jl`), `_laplace_re_precision_site` (`src/re_sd.jl`), bench wall `_S8_LATTE_A_WALL` (`bench/profile_grouped_glmm.jl`). 🌱 graft saved ~96.5k tokens this planning turn.

---

## Unlazy ledger

Created: `.unlazy/latte-kernel-after-448-20260923/GATES.md` + leaves `leaf-S0` … `leaf-S4` + `leaf-P1-DEFER-parallel`.

```sh
cd ~/local-scratch/lanes/GLLVM.jl-s9cov-20260921   # or post-merge worktree
node ~/shinichi-brain/skills/unlazy/scripts/gate-check.mjs --status --scope latte-kernel-after-448-20260923
# after a leaf:
node ~/shinichi-brain/skills/unlazy/scripts/gate-check.mjs --reverify .unlazy/latte-kernel-after-448-20260923/gates/leaf-S0-merge-448.md
```

Ensure `.unlazy/` stays gitignored.

---

## DEFER / do NOT retry

1. **Nelder–Mead demotion alone** — measured slower (8.36 s vs 4.30 s on large fixture); FORBIDDEN.
2. **Remove simplex alone** — predicts 0.032–0.046 s → **slower** than oracle; not the headline.
3. **Treat oracle as “our shape + better optimiser”** — it is scalar per-group Newton; no 200×200 Cholesky.
4. **Adopt Latte.jl / GMRF.jl / SelectedInversion.jl** — settled pre-1.0 / wrong estimand / Julia floor.
5. **`moment_start` as speed win** — blocked on NB2 correctness (variance collapse); not this arc’s first code.
6. **Public / README speed claim** before S4 identity.
7. **DRM speed6 #781 or HSquared speed12 as THIS plan’s HEADLINE.**
8. **Edits to coordinated_speed_kickoff plan file.**
9. **DRAC** unless a true job array appears; **no Actions campaigns** (D-50).
10. **Bleed into** Codex/Claude reader PRs or paste-gated DRAFT harnesses.

---

## G0 — paste ask

Shinichi, paste one line to unlock `/goal` execution:

```text
G0 next-after-448: merge #448 + kernel-first Latte arc (diag / identical log-link); NM-alone forbidden; Totoro certify
```

### Paste-ready `/goal` prompt

```text
/goal

Execute docs/dev-log/plans/2026-09-23-ultra-plan-next-after-448.md (G0 approved).

Lane: after merging itchyshin/GLLVModels.jl#448 (head was 9788fca77; advisory Frozen-R may stay red), open cursor/latte-kernel-20260923 from post-merge main. Unlazy: .unlazy/latte-kernel-after-448-20260923/

Order: S0 merge-when-green #448 → S1 paired retime (oracle ~/local-scratch/latte-prerun-20260918/runs/f3_ours_glmm.jl + shipped harness, same timing scope, ≥5 warm) → S2 ablation incl. diagonal-kernel → S3 keyword-OFF kernel prototype (diag precision; skip duplicate factor when log-link Hf≡Ho; optional fused value+grad) → S4 rtol 1e-8 identity on NB2 joint + phylo BEFORE any timing quote.

FORBIDDEN: Nelder-Mead demotion alone; remove simplex alone; Latte/GMRF adoption; public speed claim pre-S4; edit coordinated_speed_kickoff; DRAC unless array; Actions campaigns.

PARALLEL after S0: gate-check --reverify --approve --scope grouped-analytic-20260920 --jobs 1 --timeout 7200 on Totoro — READ BRANCH not directory name (s9a dir may be on exp-nm-budget).

Correction authority: ~/shinichi-brain/docs/dev-log/plan-actual/2026-09-22-next-arc-close-the-latte-gap.md §CORRECTION only. Honest gap ~4.6x; KERNEL is largest addressable item.

Verify every leaf with gate-check --reverify. Stop for Shinichi before default-ON and before any public claim.
```

---

## Status

**PAUSED — awaiting G0 paste.** No implementation in this turn.
