# After-task: S9c coverage PR #448 two-implementation merge

Date: 2026-09-22
Lane: `~/local-scratch/lanes/GLLVM.jl-s9cov-20260921` (`claude/lane-s9cov-20260921`)
HEAD at close: `e14e8b26e` (merge of `origin/main` @ `d94b01d8f` + certification repair)
PR: [#448](https://github.com/itchyshin/GLLVModels.jl/pull/448) updated; **not merged** (human/gh block)
Active lenses: Ada, Shannon, Noether, Rose (perspectives only; no spawned subagents)
Spawned subagents: none

## Scope

Main after #446/#447 had coverage fixtures and `gate_coverage`, but the suite never ran them (`_fixtures()` omitted `_coverage_fixtures()`), and `beta_trait` failed a plain `worst_rel <= RTOL_FD` check at `1.532e-06` vs `1e-6`. The #448 branch already had per-draw per-coordinate FD certification and Richardson adjudication. This slice merged both surfaces into one file and wired coverage fixtures into the in-suite path without widening `RTOL_FD`.

## Outcome

- Merged `origin/main` into the s9cov lane; resolved the single conflict in `test/test_grouped_analytic_grad.jl` by keeping both implementations.
- `_fixtures()` now ends with `_coverage_fixtures()...`; `_s9c_*` names remain as aliases/wrappers over the main `_fixture_*` / `_coverage_fixtures` surface plus the certification helpers from the branch.
- `gate_coverage` uses measured atol + Richardson for instrument-limited coordinates; `beta_trait` coord 5 is the measured case and now PASSes honestly.
- Old-base carry from the pre-main branch tip (`src/em_phylo.jl`, `test/test_sparse_phy_identities.jl`) is absent from the PR diff vs `origin/main`.

## Checks

1. `env JULIA_NUM_THREADS=4 OPENBLAS_NUM_THREADS=1 julia --project=. test/test_grouped_analytic_grad.jl --gate coverage` → `GATE G9c.1 PASS` (laptop; Totoro N).
2. `env JULIA_NUM_THREADS=4 OPENBLAS_NUM_THREADS=1 julia --project=. -e 'include("test/test_grouped_analytic_grad.jl")'` → 21/21 pass.
3. `const RTOL_FD = 1e-6` still the only FD relative bound.

## Residuals / fences

- Full `Pkg.test()` / owed `gate-check --reverify` for `grouped-analytic-20260920` not re-run here (deferred leaf; optional after human review).
- Latte-gap arc not started.
- No public speed claim.
- PR #448 left OPEN for human merge.

## Files changed (this repair)

- `test/test_grouped_analytic_grad.jl` (two-implementation merge + in-suite wire)
- `docs/dev-log/check-log.d/2026-09-22-s9c-coverage-448.md`
- `docs/dev-log/after-task/2026-09-22-s9c-coverage-448.md`
- prior lane docs already on the branch: `.unlazy/grouped-analytic-20260920/gates/leaf-S9c.md`, `docs/dev-log/after-task/2026-09-21-s9c-coverage-holes.md`
