import{_ as o,o as t,c as a,ao as s}from"./chunks/framework.uW-O5Njw.js";const m=JSON.parse('{"title":"Comparison: R, GLLVModels.jl, MixedModels.jl","description":"","frontmatter":{},"headers":[],"relativePath":"comparison.md","filePath":"comparison.md","lastUpdated":null}'),l={name:"comparison.md"};function n(r,e,i,d,c,h){return t(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="Comparison:-R,-GLLVModels.jl,-MixedModels.jl" tabindex="-1">Comparison: R, GLLVModels.jl, MixedModels.jl <a class="header-anchor" href="#Comparison:-R,-GLLVModels.jl,-MixedModels.jl" aria-label="Permalink to &quot;Comparison: R, GLLVModels.jl, MixedModels.jl {#Comparison:-R,-GLLVModels.jl,-MixedModels.jl}&quot;">​</a></h1><div class="gllvm-route gllvm-route--evidence"><div><span class="gllvm-route__eyebrow">Context, not a leaderboard</span><p>Read the timing results together with the fitted model, warm-up conditions, and the limits of each comparison.</p></div></div><p>The benchmark study (see <a href="./benchmarks">Benchmarks</a>) runs three engines on each cell to put GLLVModels.jl&#39;s speed numbers in context against the state-of-the-art Julia LMM port. The third engine is <a href="https://github.com/JuliaStats/MixedModels.jl" target="_blank" rel="noreferrer">MixedModels.jl</a>, the canonical Julia descendant of <code>lme4</code>.</p><h2 id="What-each-engine-fits" tabindex="-1">What each engine fits <a class="header-anchor" href="#What-each-engine-fits" aria-label="Permalink to &quot;What each engine fits {#What-each-engine-fits}&quot;">​</a></h2><ul><li><p><strong>R <code>gllvmTMB</code></strong> — fits the GLLVM exactly. The reference engine for every grid cell. TMB Laplace + L-BFGS in C++.</p></li><li><p><strong>GLLVModels.jl</strong> — fits the same GLLVM. Closed-form Gaussian marginal + <code>Optim</code> L-BFGS + <code>ForwardDiff</code> gradients, with a PPCA closed-form warm start for the no-other-RE path.</p></li><li><p><strong>MixedModels.jl</strong> — cannot fit a <code>K ≥ 1</code> GLLVM directly because it has no latent-factor block in the formula DSL. On <code>K = 0</code> cells the GLLVM degenerates to an LMM and the three engines coincide (the <code>K = 0</code> apples-to-apples sanity check vs <code>lme4</code> lives in <code>gllvmTMB-julia-bench/report/three-way-smoke.md</code>). On <code>K ≥ 1</code> cells MixedModels.jl fits the <strong>proxy LMM</strong> <code>value ~ 1 + (1|site)</code> (or <code>value ~ 0 + trait + (1|site)</code> when fixed effects are present) — a strictly easier problem than the GLLVM, with no rank-<code>K</code> loading parameters and no per-iteration Cholesky update on a low-rank-plus- diagonal target.</p></li></ul><p>The three-way table is reported as <em>&quot;what Julia&#39;s state-of-the-art LMM engine would do on a comparable-cost LMM problem&quot;</em> — <strong>not</strong> a direct head-to-head on the GLLVM.</p><h2 id="Headline-numbers" tabindex="-1">Headline numbers <a class="header-anchor" href="#Headline-numbers" aria-label="Permalink to &quot;Headline numbers {#Headline-numbers}&quot;">​</a></h2><p>From PERF+E (full numbers in the benchmark report):</p><ul><li><p><strong>GLLVModels.jl vs R <code>gllvmTMB</code> on the GLLVM</strong>: median speedup of <strong>~190x</strong> on the small cells, <strong>~520x</strong> on the mid cells, and <strong>~280x</strong> on the large cells. Worst-case <code>|Δ logLik|</code> across the grid: <code>2.343e-07</code>. Both engines converged on every fit.</p></li><li><p><strong>GLLVModels.jl vs MixedModels.jl on the proxy LMM</strong>: MixedModels.jl is faster on the larger cells (the proxy is a strictly easier problem), but only by a factor of <code>~1.4–1.8x</code>. The order of magnitude is the same. Where MixedModels.jl is faster, the difference is an <em>upper bound</em> on the cost the latent-factor structure adds; that the cost is bounded above by a small constant factor is itself a measure of how cleanly the closed-form Gaussian marginal eliminates the expensive part of the GLLVM evaluation.</p></li></ul><h2 id="Honest-caveats" tabindex="-1">Honest caveats <a class="header-anchor" href="#Honest-caveats" aria-label="Permalink to &quot;Honest caveats {#Honest-caveats}&quot;">​</a></h2><ul><li><p>Absolute timings in the smallest cells are sub-millisecond on the Julia side; replicate noise is on the order of single GC pauses. The benchmark uses <code>n_reps = 3</code> with medians — an order-of-magnitude scaling picture, not a publishable final number. A publishable speed claim wants <code>n_reps &gt;= 10</code> with median + MAD plus a cold-start row.</p></li><li><p>Both engines get a warm-up pass before the timed grid. The R side loads the TMB DLL and S4 method tables; the Julia side runs one per-<code>(p, n, K, has_intercept)</code> warm-up so the ForwardDiff specialisation is in the JIT cache. Without these warm-ups, rep 1 of every cell would be 10–100x slower than reps 2–3 on the engine that hadn&#39;t yet compiled the relevant code path.</p></li><li><p>MixedModels.jl&#39;s logLik on each row is on a <em>different</em> model from the GLLVM logLik. It is not directly comparable unless <code>K = 0</code>. It is reported only for completeness.</p></li></ul>',11)])])}const u=o(l,[["render",n]]);export{m as __pageData,u as default};
