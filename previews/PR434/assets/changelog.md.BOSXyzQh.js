import{_ as a,o,c as i,ao as t}from"./chunks/framework.mYIaKbGG.js";const g=JSON.parse('{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"changelog.md","filePath":"changelog.md","lastUpdated":null}'),n={name:"changelog.md"};function r(s,e,d,l,c,p){return o(),i("div",null,[...e[0]||(e[0]=[t('<h1 id="Changelog" tabindex="-1">Changelog <a class="header-anchor" href="#Changelog" aria-label="Permalink to &quot;Changelog {#Changelog}&quot;">​</a></h1><p>Notable changes to GLLVModels.jl. Style mirrors <code>gllvmTMB</code>&#39;s NEWS: status labels <strong>IN</strong> (shipped), <strong>PARTIAL</strong> (limited), <strong>PLANNED</strong> (next), with issue/PR refs.</p><h2 id="GLLVModels.jl-development-version" tabindex="-1">GLLVModels.jl (development version) <a class="header-anchor" href="#GLLVModels.jl-development-version" aria-label="Permalink to &quot;GLLVModels.jl (development version) {#GLLVModels.jl-development-version}&quot;">​</a></h2><h3 id="Changed" tabindex="-1">Changed <a class="header-anchor" href="#Changed" aria-label="Permalink to &quot;Changed {#Changed}&quot;">​</a></h3><ul><li><p><strong>CHANGED:</strong> the package and module are now named <code>GLLVModels</code>; the public fitting API remains unchanged. After the rename, <code>using GLLVM</code> cannot resolve a package. Once <code>GLLVModels</code> is loaded, <code>GLLVModels.GLLVM</code> is only a temporary qualified alias; it does not provide import compatibility and is deprecated.</p></li><li><p><strong>Development:</strong> fixed Gaussian source fits accept a complete mean design <code>X</code>. Explicit-source wide/long formulas expose trait intercepts, shared slopes, categorical contrasts and interactions. Fits retain the copied design, response shape and coefficient labels. No R source-grammar, bridge, random-slope or calibrated-uncertainty claim is implied.</p></li><li><p><strong>Development:</strong> per-trait Gaussian fitting accepts an explicit fixed residual SD and estimates unique variances within that constraint. Results retain unique and total diagonal variances; existing defaults and parameter counts are unchanged. The <code>pervar=true</code> Gaussian formula route preserves trait intercepts, zero-mean designs, shared slopes and categorical contrasts. Bridge and interval parity are not established. AGHQ requests on the fixed-residual unique model retain exact Gaussian/Laplace with warning and provenance; <code>k=1</code> is quiet. Plain heteroscedastic AGHQ remains unimplemented and is labeled separately.</p></li><li><p><strong>PARTIAL:</strong> <code>SourceCovariance</code> and <code>fit_gaussian_sources</code> fit fixed Gaussian covariance among source groups. Targeted tests and six retained public-R comparisons pass; bridge support and calibrated uncertainty remain unverified. The unique-variance comparison has nearly singular curvature.</p></li><li><p>Exact Gaussian fits and Wald/profile intervals handle an empty fixed-effect design. Failed profile refits cannot supply finite bounds without a finite likelihood crossing.</p></li><li><p>Formula fits validate every supplied site-table column before response access, including intercept-only fits. Empty tables remain valid without covariates. The original NB2 wide/long formula model matches its native fit; broader interface qualification remains incomplete.</p></li><li><p><strong>EXPERIMENTAL:</strong> ordinary NB2 evaluates its density directly from the mean and uses an overflow-safe observed curvature. The original paired fit, scalar derivatives and required NB2/truncated-NB2 runner pass. Broader recovery, full package checks and independent review remain pending.</p></li><li><p>Student-t fitting rejects infinite fixed degrees of freedom before reading responses, for scalar and per-trait inputs. Finite positive fixed values and the estimated-df route are unchanged.</p></li><li><p><strong>EXPERIMENTAL:</strong> grouped Tweedie distinguishes fixed common, shared estimated and per-species estimated power; <code>TweediePerTraitPowerFit</code> stores the latter. Student fits record whether degrees of freedom were estimated so information-criterion parameter counts are correct. These changes do not establish full parity: the recorded Student-t R comparison still does not converge reliably, and broader qualification remains pending.</p></li><li><p>Branch-RE uses an equivalent dense marginal fallback when the auxiliary sparse precision is numerically unsafe. This preserves valid marginal models without a ridge, at a possible O(p²) memory cost reported by a warning.</p></li><li><p><strong>CHANGED:</strong> Gamma&#39;s Laplace <strong>log-determinant now uses the observed conditional curvature</strong> <code>α·y/μ</code>, matching TMB and therefore <code>gllvmTMB</code>. It previously used the Fisher (expected) weight — the constant <code>α</code>, which is exactly the <em>expectation</em> of the correct one. <strong>This moves reported <code>loglik</code> values for Gamma fits</strong>, and anything derived from the Hessian such as Wald standard errors; point estimates move very little, because the conditional mode is determined by the score, which is unaffected. Measured against numerical quadrature the observed curvature is closer <strong>12/12</strong> seeds, with 20–60× smaller error. <code>hessian = :fisher</code> restores the previous behaviour. This was <strong>instance 8</strong> of a Fisher-vs-observed fault class and the worst-placed one: <code>fit_gllvm(Y; family = Gamma())</code> reaches it, because Gamma is deliberately excluded from the <code>disp_group = :species</code> auto-coercion that shields the other dispersion families. <strong>Per-family, not a global switch.</strong> Other families keep the Fisher default: measured evidence says observed is <em>not</em> closer for Beta (2/12) and is measurably worse for GP-1&#39;s dispersion recovery. See <code>docs/design/capability-status.md</code> § <em>Laplace curvature</em> for the family-by-family table and which rows will not match <code>gllvmTMB</code> to machine precision.</p></li></ul><h3 id="Fixed" tabindex="-1">Fixed <a class="header-anchor" href="#Fixed" aria-label="Permalink to &quot;Fixed {#Fixed}&quot;">​</a></h3><ul><li><strong>FIX:</strong> every non-Gaussian <strong>Wald</strong> standard error was wrong. The observed-information finite-difference Hessian (<code>_fd_hessian</code>, backing <code>confint(fit, Y; method=:wald)</code> and <code>_family_wald</code> for Poisson / Binomial / NB / Beta / Gamma / Tweedie / two-part / SPDE-latent / structural CIs) wrote <code>2f0</code> — which Julia lexes as the Float32 literal <code>2.0f0</code>, not <code>2 * f0</code> — so the diagonal second difference dropped the centre value and exploded with the objective&#39;s large constant, collapsing <code>inv(H)</code> to standard errors ~1e-6. Off-diagonals and the profile/bootstrap routes were unaffected. Added <code>test/test_fd_hessian.jl</code> pinning the Hessian to a known analytic value (the existing CI tests only checked structure / <code>pd_hessian</code>, never SE magnitude, so the bug was invisible).</li></ul><h3 id="Engine" tabindex="-1">Engine <a class="header-anchor" href="#Engine" aria-label="Permalink to &quot;Engine {#Engine}&quot;">​</a></h3><ul><li><p><strong>IN:</strong> profile-likelihood hardening for admitted LV-effect routes: <code>profile_ci</code>, non-Gaussian <code>confint(...; method=:profile)</code>, and <code>confint_lv_effects(...; method=:profile)</code> now expose bounded refit/profile controls. <code>confint_lv_effects</code> also accepts <code>profile_indices</code> to profile selected entries of <code>vec(B_lv)</code> without running the whole trait-effect matrix.</p></li><li><p><strong>IN:</strong> phylogenetic GLM (<code>fit_phylo_glm</code> / <code>PhyloGLMFit</code>) — a per-species phylogenetic random intercept for the non-Gaussian families (Poisson / NB / Binomial) via an augmented-state joint Laplace over the sparse phylogenetic precision.</p></li><li><p><strong>IN:</strong> zero-inflated binomial (<code>fit_zib_gllvm</code> / <code>ZIBFit</code>), with Wald / profile / bootstrap confidence intervals.</p></li><li><p><strong>IN:</strong> negative-binomial type-1 (NB1, linear variance <code>Var = μ(1+φ)</code>; <code>fit_nb1_gllvm</code>).</p></li></ul><h3 id="Documentation" tabindex="-1">Documentation <a class="header-anchor" href="#Documentation" aria-label="Permalink to &quot;Documentation {#Documentation}&quot;">​</a></h3><ul><li><strong>IN:</strong> pkgdown-style documentation site (DocumenterVitepress) — dropdown navbar, full-text search, light/dark mode; homepage mirrors <code>gllvmTMB</code>&#39;s with a Julia flavour.</li></ul><h3 id="Quality-and-infrastructure" tabindex="-1">Quality &amp; infrastructure <a class="header-anchor" href="#Quality-and-infrastructure" aria-label="Permalink to &quot;Quality &amp; infrastructure {#Quality-and-infrastructure}&quot;">​</a></h3><ul><li><p><strong>IN:</strong> <code>Pkg.test()</code> adopted as the full-suite command; Aqua (package hygiene) and JET (type-stability of the O(p) kernels) run in CI.</p></li><li><p><strong>IN:</strong> isolated RCall.jl parity scaffold (<code>test/parity/</code>, opt-in) for checking agreement against R <code>gllvmTMB</code>.</p></li></ul><h2 id="GLLVModels.jl-v0.2.0" tabindex="-1">GLLVModels.jl v0.2.0 <a class="header-anchor" href="#GLLVModels.jl-v0.2.0" aria-label="Permalink to &quot;GLLVModels.jl v0.2.0 {#GLLVModels.jl-v0.2.0}&quot;">​</a></h2><p>A large expansion from the v0.1.0 Gaussian-only pilot to a broad, gllvmTMB-class package; every numerical addition is gated by deterministic tests.</p><h3 id="Response-families" tabindex="-1">Response families <a class="header-anchor" href="#Response-families" aria-label="Permalink to &quot;Response families {#Response-families}&quot;">​</a></h3><ul><li><p><strong>IN:</strong> broad one-part GLM family set via a Laplace marginal — Poisson, negative binomial (NB2), Binomial / Bernoulli, Beta, Gamma, Exponential, Ordinal, and Tweedie (compound Poisson–Gamma, <code>fit_tweedie_gllvm</code>).</p></li><li><p><strong>IN:</strong> two-part / mixture families — Delta-lognormal, Delta-Gamma, Hurdle-Poisson, Hurdle-NB, beta-hurdle (<code>fit_beta_hurdle_gllvm</code>), ordered-beta (<code>fit_ordered_beta_gllvm</code>), ZIP, and ZINB.</p></li><li><p><strong>IN:</strong> links — identity, log, logit, probit, cloglog.</p></li></ul><h3 id="Estimators-and-inference" tabindex="-1">Estimators &amp; inference <a class="header-anchor" href="#Estimators-and-inference" aria-label="Permalink to &quot;Estimators &amp; inference {#Estimators-and-inference}&quot;">​</a></h3><ul><li><p><strong>IN:</strong> a variational (VA / ELBO) estimator alongside Laplace (<code>fit_*_gllvm_va</code>), with analytic inner gradients and envelope-theorem outer gradients for the Gauss–Hermite families, and <strong>VA-based standard errors</strong> (<code>confint(...; objective=:va)</code>).</p></li><li><p><strong>IN:</strong> confidence-interval routes — Wald, profile likelihood, and parametric bootstrap — for the GLM and two-part families via <code>confint(fit, Y; method=…)</code>, plus a tidy <code>coef_table</code>; public bridge promotion remains status-tracked by family/structure. Faster profile CIs use false-position on <code>√D</code>.</p></li></ul><h3 id="Structure,-covariates,-ordination" tabindex="-1">Structure, covariates, ordination <a class="header-anchor" href="#Structure,-covariates,-ordination" aria-label="Permalink to &quot;Structure, covariates, ordination {#Structure,-covariates,-ordination}&quot;">​</a></h3><ul><li><p><strong>PARTIAL:</strong> predictor-informed latent-score means for the ordinary unit-tier path, with <code>fit_gaussian_gllvm(...; X_lv=...)</code>, <code>fit_poisson_gllvm(...; X_lv=...)</code>, <code>fit_nb_gllvm(...; X_lv=...)</code>, <code>fit_gamma_gllvm(...; X_lv=...)</code>, <code>fit_beta_gllvm(...; X_lv=...)</code>, <code>fit_binomial_gllvm(...; X_lv=...)</code>, and <code>fit_ordinal_gllvm(...; X_lv=...)</code> for complete-response Gaussian, Poisson (log link), shared-dispersion NB2, shared-shape Gamma, shared-precision Beta, binomial logit/probit/cloglog, and shared-cutpoint Ordinal logit point fits. <code>getLV(...; component=:mean/:innovation/:total)</code> reports score decompositions, while <code>extract_lv_effects()</code> reports the rotation-stable induced trait effect <code>B_lv = Λ * alpha_lv&#39;</code> by default and the rotation-dependent CLV-style <code>alpha_lv</code> table with <code>type=:axis_effect</code>. <code>confint_lv_effects()</code> supplies uncertainty for <code>B_lv</code>, not for <code>alpha_lv</code>. Response masks, simultaneous fixed-effect <code>X</code>, raw axis-effect SEs, per-trait ordinal bridge parity, W-tier, phylogenetic/source-specific extensions, and R-package row promotion remain gated.</p></li><li><p><strong>IN:</strong> fixed-zero shared covariate coefficients for Gaussian (<code>β_fixed</code>) and non-Gaussian (<code>γ_fixed</code>) fixed-effect-X fits, plus bridge status fields for <code>gllvmTMB</code>&#39;s <code>Xcoef_fixed</code> contract.</p></li><li><p><strong>IN:</strong> environmental covariates — shared-<code>γ</code> (<code>fit_gllvm_cov</code>) and species-specific <code>B</code> (<code>fit_gllvm_speciescov</code>); fourth-corner trait–environment interactions (<code>fit_fourthcorner_gllvm</code>); fixed community row effects (<code>fit_roweffect_gllvm</code>); quadratic response (<code>fit_quadratic_gllvm</code>).</p></li><li><p><strong>IN:</strong> the ordination trio — unconstrained, concurrent (<code>num.lv.c</code>, <code>fit_concurrent_gllvm</code>), and constrained / RRR (<code>num.RR</code>, <code>fit_rrr_gllvm</code>); <code>ordination</code> / <code>ordiplot</code> / <code>select_lv</code> post-fit helpers.</p></li></ul><h3 id="Spatial-and-missing-data" tabindex="-1">Spatial &amp; missing data <a class="header-anchor" href="#Spatial-and-missing-data" aria-label="Permalink to &quot;Spatial &amp; missing data {#Spatial-and-missing-data}&quot;">​</a></h3><ul><li><p><strong>IN:</strong> SPDE / Matérn-GMRF spatial field (Lindgren–Rue–Lindström 2011) — <code>fit_spde_gaussian</code> and the SPDE field as a latent variable inside the non-Gaussian GLLVM (<code>fit_spde_latent_gllvm</code>), with kriging prediction (<code>predict_spatial</code>) and auto-meshing (<code>spde_mesh_grid</code> / <code>spde_mesh_delaunay</code>).</p></li><li><p><strong>IN:</strong> NA handling in the Laplace core (<code>marginal_loglik_laplace(...; mask)</code>), with <code>observed_mask(Y)</code> and <code>missing</code> support in the GLM fitters.</p></li></ul><h3 id="Interface" tabindex="-1">Interface <a class="header-anchor" href="#Interface" aria-label="Permalink to &quot;Interface {#Interface}&quot;">​</a></h3><ul><li><strong>IN:</strong> the <code>@formula</code> / <code>gllvm(...)</code> front-end (continuous fixed effects, wide <ul><li>long input) and an R interface scaffold (<code>r/gllvmjl.R</code>, via JuliaConnectoR).</li></ul></li></ul><h2 id="GLLVModels.jl-v0.1.0" tabindex="-1">GLLVModels.jl v0.1.0 <a class="header-anchor" href="#GLLVModels.jl-v0.1.0" aria-label="Permalink to &quot;GLLVModels.jl v0.1.0 {#GLLVModels.jl-v0.1.0}&quot;">​</a></h2><ul><li><p><strong>IN:</strong> Gaussian + phylogenetic GLLVM engine — closed-form marginal likelihood, PPCA / EM initialisation, multiple phylogenetic representations (sparse precision, Felsenstein contrasts, edge-incidence) agreeing to machine precision.</p></li><li><p><strong>IN:</strong> Wald / profile-likelihood / parametric-bootstrap confidence intervals, including derived quantities (Σ_y entries, communality, cross-trait correlation, phylogenetic signal).</p></li><li><p><strong>IN:</strong> ~340× median per-fit speedup over R <code>gllvmTMB</code> on the Gaussian benchmark grid, reproducing estimates and likelihoods to machine precision. <em>(Correction appended 2026-08-25 — the entry is left as originally published rather than rewritten. &quot;Machine precision&quot; overstates it: the measured worst case across that grid is <code>|Δ logLik| = 2.343e-07</code> and <code>Σ_y</code> relative Frobenius <code>4.424e-05</code>, i.e. agreement to at least six significant digits, not to ~2.2e-16. The <code>~340×</code> figure is also specific to the single-σ² Gaussian closed-form path and does NOT generalise: measured non-Gaussian speedups include truncated_poisson ≈2.2× and Gamma ≈1.6×.)</em></p></li></ul>',27)])])}const m=a(n,[["render",r]]);export{g as __pageData,m as default};
