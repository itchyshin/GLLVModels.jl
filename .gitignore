Manifest.toml
*.cov
/docs/build
/docs/node_modules
/docs/package-lock.json
/docs/src/.vitepress/cache
/docs/src/.vitepress/.temp
/benchmarks/*.json
.DS_Store

# Personal notes — must never be tracked / uploaded to public repos
*russell*
*Russell*
*Dinnage*

# campaign probe outputs (scratch)
campaigns/curvature_adjudication/prerun_out/
# Local execution evidence and mutable acceptance state (never publish raw paths).
.unlazy/
.unlazy-hook-state.json

# Local Python tool bytecode (not source evidence)
/tools/__pycache__/
/tools/tests/__pycache__/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
